# Fichier de configurations des options de demarrage pour le serveur, et par le client
# Groupe concernee par la configuration des options
[mysqld]
user = root
datadir = /var/lib/mysql/
port = 3306
log-bin = /var/lib/mysql-bin
bind-address = 0.0.0.0
skip-networking = false